import { Typography } from '@mui/joy';
import { Page } from '../common';

export default function Processes() {
  return (
    <Page>
      <Typography variant="plain">Processes</Typography>
    </Page>
  );
}
